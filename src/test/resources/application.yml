server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wedding_registry
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

#logging:
#  level:
#    org.hibernate.SQL: debug
